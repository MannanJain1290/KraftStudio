{
  "name": "kraft-studio-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend -- --host",
    "dev:frontend": "npm run dev --prefix frontend -- --host",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "dependencies": {
    "resend": "^6.22.0",
    "socket.io": "^4.8.3",
    "socket.io-client": "^4.8.3"
  }
}
